using UnityEngine;

public class VictoryScreen : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // GameObject.FindGameObjectWithTag("BGM").GetComponent<BGMusic>().StopMusic();
    }
}
